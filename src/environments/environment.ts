export const environment = {
  production: false
};

// This file is replaced during build by the `fileReplacements` array in `angular.json`.
// When building for production, this file is used instead of `environment.ts`.
// The list of file replacements can be found in `angular.json`.
